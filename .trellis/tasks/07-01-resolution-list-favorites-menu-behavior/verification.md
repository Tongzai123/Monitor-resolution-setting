# 分辨率列表收藏菜单行为调整验证记录

## 当前状态

- 记录时间：2026-07-01
- 已完成代码实现：隐藏刷新率列表去重、隐藏刷新率选择最高刷新率、默认关闭显示刷新率、单显示器右键菜单扁平化、收藏入口跳过确认/回滚。
- 未执行真实显示器手工验证；需要在运行中的 Electron 应用里确认托盘面板和右键菜单实际行为。

## 已执行验证

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 主进程语法检查 | `node --check src/electron.js` | 通过 |
| 面板 preload 语法检查 | `node --check src/panel-preload.js` | 通过 |
| 前端构建和 JSX 转译 | `npm run parcel-build` | 通过 |

## 构建提示

`npm run parcel-build` 通过，但输出了既有依赖数据过期提示：

- `baseline-browser-mapping` 数据超过两个月。
- `caniuse-lite` / Browserslist 数据超过八个月。

这些提示不影响本次构建结果；本任务未安装依赖、未更新锁文件。

## 待手工验收

- 关闭“显示刷新率”后，托盘面板同一分辨率只显示一项。
- 关闭“显示刷新率”后，选择某分辨率时实际切到该分辨率最高刷新率。
- 开启“显示刷新率”后，可重新看到并选择具体刷新率组合。
- 单显示器时，右键托盘菜单“分辨率”直接显示收藏项。
- 多显示器时，右键托盘菜单保留显示器名称分层。
- 从面板收藏项、右键菜单收藏项、快捷键收藏项切换时不出现确认/回滚条。
- 从面板非收藏项切换时仍出现确认/回滚条，面板关闭后仍能自动回滚。
