# 分辨率设置与收藏设计

## 设置位置

设置页优先放在 `monitors` 页面，因为分辨率控制属于显示器管理能力。高级选项可放到 debug 或展开区域。

## 设置字段

字段以 `resolution*` 前缀命名，避免和现有 brightness/profile/hotkey 设置混淆。

## 收藏匹配

收藏存储使用稳定显示器 key。匹配失败时，不删除收藏，只在当前设备不可用时隐藏或标记不可用。

## 场景预设

首版只保留 `resolutionPresets` 数据结构，不提供完整编辑器和自动应用。
