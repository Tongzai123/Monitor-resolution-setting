# 分辨率设置与收藏实施计划

1. 在默认设置中增加 `resolution*` 字段。
2. 在设置页新增分辨率区域和选项。
3. 在模式列表中增加收藏/取消收藏操作。
4. 按显示器保存收藏。
5. 接入筛选逻辑：低刷新率、全部模式、只显示收藏。
6. 验证设置持久化和重启恢复。

## 验证

- `npm run parcel-build`
- `npm run dev`
- 手工切换设置、收藏模式、重启应用后检查状态。
