# 分辨率主面板接入实施计划

1. 在 preload 中暴露模式列表、应用、确认、还原接口。
2. 在面板中读取 monitor 的当前分辨率字段。
3. 新增分辨率行 UI。
4. 新增模式下拉列表 UI。
5. 接入确认条和倒计时广播。
6. 补充 SCSS，确保紧凑、不遮挡、不溢出。
7. 手工验证不同显示器数量和面板高度。

## 验证

- `npm run parcel-build`
- `npm run dev`
- 手工打开/关闭面板、展开模式列表、执行切换和回滚。
